# 基于 NL2MQL2SQL 与多 Agent 协同的智能报告生成系统可行性分析报告

## 一、项目背景与概述

随着企业数字化转型的深入，将底层结构化业务数据转化为高层决策洞察的“数据到报告（Data-to-Insight）”需求日益迫切。传统报告撰写面临数据链路长、手工装配耗时、缺乏动态归因以及因人工校对失误导致的数据合规风险等痛点。

本项目旨在依托现有的、已通过业务验证的 NL2MQL2SQL 引擎，构建一套基于大语言模型（LLM）的多 Agent 协同（Multi-Agent Orchestration）智能报告生成系统。系统通过“5 步主干流水线 + 3 个旁路控制插件”的解耦架构，配合“人在回路（Human-in-the-Loop, HITL）”机制，实现逻辑严密、书面正式、数据零幻觉（Zero-Hallucination）的企业级智能报告自动化全生命周期管理。

## 二、核心架构设计与关键组件说明

系统整体采用**单向线性主干流（Linear Workflow）搭配微服务化旁路插件（Middleware Plugins）**的拓扑结构。在确保工程状态管理（State Management）极简、系统复杂度可控的前提下，最大化提升报告的专业深度。

### 1. 5 步主干流水线（The Main Pipeline）

#### Agent 1：意图识别与大纲生成器（The Planner）

- **职责：** 解析用户自然语言输入的空间、时间及场景实体，调用**企业报告模版库（Enterprise Report Template Library）**动态生成报告大纲。

#### Agent 2：章节拆解与数据语义规约生成器（The Data Specifier）

- **职责：** 逐章分析大纲，将各章节的非结构化叙述诉求转化为多条独立的、面向底层引擎的数据语义规约（Data Schema Specification），即结构化的自然语言取数指令。

#### Agent 3：数据提取执行器（The Data Retriever）

- **职责：** 作为确定性执行层，直接对接现有的 NL2MQL2SQL 引擎，批量或串行驱动数据库查询，返回结构化 JSON/DataFrame 数据。

#### Agent 4：章节文本生成器（The Draft Writer）

- **职责：** 采用检索增强生成（RAG）或上下文学习（In-Context Learning）技术，结合章节主题与结构化数据进行文本解译，将数字转化为严谨的书面公文语，并输出前端图表渲染配置（如 ECharts JSON）。

#### Agent 5：全局统稿与对齐路由器（The Editor-in-Chief）

- **职责：** 消除各章节**多模态文本片段（Multimodal Text Fragments）**之间的语气不一致与逻辑断层，进行上下文承上启下的衔接与文本平滑处理。

### 2. 深度解析：企业报告模版库（Enterprise Report Template Library）

企业报告模版库是整个智能化生产线的“骨架驱动中心”，它是一套将企业内各类严肃报告（如账户运行报告、风险月报）的组织架构、逻辑流转和数据诉求进行**标准化、机器可读化（Machine-Readable）**定义的元数据配置中心。

#### 业务边界划分

- **模版库内部（库内资产）：** 仅包含报告的静态逻辑框架（Schema）、章节树形结构（AST Tree）、各章节标准文本样式（CSS/Markdown Style），以及该章节推荐绑定的底层数据标签（Data Tagging）。
- **模版库外部（运行时数据）：** 不包含任何具体的时间范围、地区参数或实际业务数字。动态变量（如“XXX 地区”“2026 年 Q2”）完全由 Agent 1 在运行时动态注入。

#### 核心复用的 LLM 能力

- **少样本提示（Few-Shot Prompting）能力：** 模版库内沉淀了高质量的行业公文样本。Agent 1 调取模版时，会同步捞出该场景的 Few-Shot 样本，约束大模型生成符合行业标准的规范大纲。
- **参数化槽位填充（Slot Filling）能力：** 模版大纲定义了标准槽位（如 `[Target_Region]`、`[Report_Period]`），LLM 需发挥实体对齐能力，将用户输入的模糊文本精准注入到大纲槽位中。

### 3. 3 个旁路增强插件（The Middleware Plugins）

#### Gatekeeper 插件（口径合规与术语对齐）

在 Agent 2 与 Agent 3 之间，维护企业统一指标字典，将模糊的自然语言需求（如“大额交易”）改写为高精准的结构化条件约束（如“单笔金额 > 500,000 CNY”），作为 NL2MQL2SQL 的前置过滤器。

#### Analyst 插件（自动化统计异常检测与断言机制）

在 Agent 3 与 Agent 4 之间，对提取的数据进行自动化统计异常检测与断言（Statistical Anomaly Detection & Assertion）。

- **深度原理解析：** 所谓“统计学秒批”，是指数据返回后，插件在毫秒级内运行基线比对。利用同比/环比分析、移动平均（Moving Average）、Z-score 标准差检验等统计学手段，自动识别数据中的离群点（Outliers）、突增突降或触及红线的异常指标（Assertion Violation）。
- **后续联动：** 一旦检测到异常，Analyst 插件会自动触发多轮下钻（Drill-down）追问，或者在本地非结构化知识库（RAG）中检索该时间段的外部事件通知（如政策变更、系统维护日志），将结构化异动与非结构化归因融合，为 Agent 4 供应具备“深层因果关系”的写作素材。

#### Auditor 插件（事实一致性交叉审计）

在 Agent 5 输出后，执行代码级别的硬核校验（Hard Verification），通过正则表达式抽取最终文本中的数字，并与 Agent 3 的原始 JSON 进行精准的**数据血缘（Data Lineage）**对齐。它是确保“零幻觉”交付的安全熔断器。

## 三、人机协同（Human/Machine-in-the-Loop）分工蓝图

为平衡系统自动化效率与金融级合规风险，系统将控制边界清晰地划分为两层：

### 1. 人在回路（Human-in-the-Loop, HITL）：方向锚定与价值判断

- **结构化大纲与口径门禁卡点（Gateway Review）：** 在 Agent 1 和 Agent 2 生成大纲、Gatekeeper 转化标准指标口径后，系统暂停流转并呼叫人类。业务人员进行可视化勾选确认（用时约 10～30 秒），确认无误后方可释放高负载的数据查询。
- **终稿发布前的政策性定调（Tone Alignment）：** 报告产出后由业务人员进行最终审阅。利用人类智慧，对敏感或负面业务数据（如业务阶段性下滑）进行合规、建设性的政治与业务文字定调。
- **元数据及指标知识库的日常喂养：** 技术与业务人员定期手动更新术语字典、指标口径与最新合规新规文档，作为系统持续迭代的底层冷启动输入。

### 2. 机器在回路（Machine-in-the-Loop, MITL）：自动化纠错与对抗博弈

- **NL2MQL2SQL 语法自愈（Self-Correction）：** Agent 3 捕获数据库或执行引擎返回的 Error Trace，在后台自我重试改写（限制最多 3 次），可自主解决 80% 以上因符号配对或轻微字段映射偏差导致的语法错误。
- **对抗式数据审计（Adversarial Auditing）：** Auditor 插件一旦发现最终文本中的数字与原始 JSON 存在血缘不一致，直接拦截并触发退回机制，向 Agent 5 抛出冲突 Prompt 并强制重写，在后台实现机器自循环，直至数据 100% 准确性闭环。

## 四、项目可行性评估（技术、业务、工程）

### 1. 技术可行性（High）

- **稳固的底层基座：** 项目中最核心、技术门槛最高的“结构化数据提取（NL2MQL2SQL）”环节已在近期通过多轮真实的业务验证，具备极高的确定性。
- **解耦性拓扑：** 采用“主干 + 微服务插件”架构，避免了由于构建复杂状态机（State Machine）而导致的 Agent 死锁或无休止对话（Infinite Loops），工程可控性高。
- **防幻觉闭环：** 通过为每个 JSON 字段绑定 UUID 与 SQL 语句，实现数据的“影子追踪（Lineage Tracking & Citation）”，从底层架构上杜绝了大模型的文本幻觉。

### 2. 业务可行性（High）

- **痛点契合度高：** 完美切中金融及大型企业客户对于定期报告撰写中“耗时冗长、维度干瘪、人工易错”的刚性痛点。
- **渐进式交付（Phased Rollout）保障：** 业务推进路线清晰，可实施分阶段释放价值的 MVP 策略：

  1. **第一阶段（效能提效）：** 团队内部的研发效能/运维状态周月报（低业务风险，验证 MITL 自愈链路）。
  2. **第二阶段（管理赋能）：** 面向管理层的业务诊断/复盘报告（引入 Analyst 自动化统计异常检测与自动下钻）。
  3. **第三阶段（核心交付）：** 面向外部金融客户的交付运营白皮书/服务报告（全链路成熟，强证据引用交付）。

### 3. 工程复杂度与成本可行性（Controllable）

- **单点调试成本低：** 3 个增强角色被设计为“插拔式插件”而非全串行的大模型，工程开发人员可对其进行独立的 Prompt 单元测试，极大降低了分布式 Agent 系统级联失效的风险。
- **算力成本优化：** 引入 HITL（人在回路）前置口径确认机制，避免了因自然语言歧义而导致的大规模无效 SQL 扫表与 LLM 算力浪费，Token 成本及计算资源显著可控。

## 五、结论与后续行动建议

基于 NL2MQL2SQL 引擎已经跑通且收敛的技术事实，本项目构建的多 Agent 智能报告生成系统在技术、业务及工程层面均展现出极高的可行性。

### 近期行动项（Action Items）

1. **定义统一的元数据血缘协议：** 优先制定从 Agent 3 到 Agent 5 之间流转的 JSON 数据契约，确保数据字段中强制包裹 UUID、原始 SQL 及指标统计口径标定（Data Tagging）。
2. **启动 MVP 研发：** 依托内部“研发效能周报”场景，采用 5 步主干流程进行小规模上线，验证 Agent 2 数据语义规约拆解的准确率，随后逐步接入 3 个旁路控制插件。
